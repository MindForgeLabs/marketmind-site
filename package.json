{
  "name": "marketmind-site",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "start": "npm run start --workspace web",
    "lint": "npm run lint --workspace web",
    "type-check": "npm run type-check --workspace web",
    "format": "npm run format --workspace web",
    "storybook": "npm run storybook --workspace web",
    "build-storybook": "npm run build-storybook --workspace web",
    "test": "npm run test --workspace web",
    "test:watch": "npm run test:watch --workspace web",
    "test:unit": "npm run test:unit --workspace web",
    "test:ui": "npm run test:ui --workspace web",
    "test:ci": "npm run test:ci --workspace web",
    "test:sb": "npm run test:sb --workspace web"
  }
}
