// src/app/docs/quickstart/page.mdx
export const metadata = {
  title: "Quickstart",
  description: "Install the client, run a health check, and execute your first MarketMind strategy.",
};

# Quickstart

This guide walks you through a minimal MarketMind setup:

1. Install the CLI or client.
2. Run a health check.
3. Execute a simple strategy pipeline.
4. See where to go next.

> **Note**
> CLI names and flags below are examples. Replace them with your real commands once your tooling is finalized.

---

## 1. Install the CLI

```bash
# Example placeholder – change to your actual package
pip install marketmind
